#include <bits/stdc++.h>
using namespace std;

int main(){
    
    //Everything same but it didn't store in sorted order

    // All operations are in O(1) time, only in wrost case it take O(n) time
    return 0;
}