#include <bits/stdc++.h>
using namespace std;

int main(){
    
    // only stores unique but not in sorted order
    unordered_set<int> uos;

    // lower_bound() & upper_bound() function not work, rest all are same

    //Most of the opeartions are in of O(1) time but in the wrost case that takes O(n) time
    return 0;
}