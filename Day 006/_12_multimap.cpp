#include <bits/stdc++.h>
using namespace std;

int main(){
    
    //Everything same as map, only it can store duplicate keys in sorted order

    //Only map[] function can't be used 
    return 0;
}